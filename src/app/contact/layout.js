export const metadata = {
  title: "Contact Us — Token Calculator",
  description:
    "Get in touch with the Token Calculator team for bug reports, feature requests, partnerships, or privacy inquiries.",
  alternates: { canonical: "/contact" },
};

export default function ContactLayout({ children }) {
  return children;
}
